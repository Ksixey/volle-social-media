import React from 'react';
import classes from './PageNotFound.module.css'

export const PageNotFound =() =>{
    return <div className={classes.pageNotFoundContainer}>
        <h1  className={classes.pageNotFoundTitle}>404 not found</h1>
    </div>
}